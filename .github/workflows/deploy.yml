name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy handled by server-side cron
        run: echo "Server polls GitHub every 2 minutes and self-deploys. No action needed here."
